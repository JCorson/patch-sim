[project]
name = "ap_sim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "matplotlib",
    "pandas",
]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = []

[project.optional-dependencies]
dev = ["ruff", "pytest", "mypy", "pandas-stubs"]

[tool.uv.sources]
ap_sim = {path = "./ap_sim", editable = true}

[tool.pytest.ini_options]
testpaths = ["tests"]